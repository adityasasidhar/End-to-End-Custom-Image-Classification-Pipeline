[project]
name = "customimageclassificationpipeline"
version = "0.1.0"
description = "This is a fun learning attempt at building custom models, I was bored and the year was new, so i thought why not ?? hehehehe"
authors = [
    { name = "Aditya Sasidhar" }
]
license = { text = "MIT" }
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.6",
    "opencv-python>=4.12.0.88",
    "pyyaml>=6.0.3",
    "torch>=2.9.1",
    "torchvision>=0.24.1",
    "tqdm>=4.67.1",
]
